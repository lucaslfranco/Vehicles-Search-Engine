# Vehicles-Search-Engine (IA Project)

``` bash
### To run the web crawler and colect data, run:
scrapy crawl cars -a car="CAR_MODEL" -o cars.json

### To run the web server, in localhost:3000, run:
npm start
```
